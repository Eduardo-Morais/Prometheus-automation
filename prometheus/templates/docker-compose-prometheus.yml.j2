services:
  prometheus:
    image: prom/prometheus
    container_name: prometheus
    volumes:
      - /home/{{ ansible_user_id }}/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - /home/{{ ansible_user_id }}/prometheus/alerts.yml:/etc/prometheus/alerts.yml
    ports:
      - "9090:9090"

    environment:
      - TIMEZONE=America/Recife

    restart: always
